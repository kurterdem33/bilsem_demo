import 'package:flutter/material.dart';

class AppColors {
  // Tasarımdaki Canlı Tonlar
  static const Color primary = Color(0xFF2D9CDB); // Canlı Mavi (Sayı Hafızası)
  static const Color success = Color(0xFF27AE60); // Canlı Yeşil (Nasıl Oynanır)
  static const Color accent = Color(0xFFF2994A);  // Sıcak Turuncu (Ayarlar)

  // Destekleyici Koyu Renkler (Yazılar için)
  static const Color textDark = Color(0xFF1E1E1E);
  static const Color textGrey = Color(0xFF828282);

  // Arka Planlar
  static const Color background = Color(0xFFFFFFFF);
  static const Color disabled = Color(0xFFE0E0E0);
}